# Default values for llama-stack-instance
# This is a YAML-formatted file.

tenant:
  username: user1
  domain: apps.cluster.example.com

# Model configuration
model:
  name: nvidia-nemotron-nano-9b-v2-fp8
  displayName: NVIDIA Nemotron Nano 9b v2 FP8
