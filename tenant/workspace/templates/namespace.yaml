---
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespacePrefix }}-{{ .Values.tenant.username }}
  labels:
    app.kubernetes.io/part-of: che.eclipse.org
    app.kubernetes.io/component: workspaces-namespace
    opendatahub.io/dashboard: "true"
  annotations:
    openshift.io/display-name: "Workspace {{ .Values.tenant.username }}"
    openshift.io/description: "Workspace Namespace"
    che.eclipse.org/username: {{ .Values.tenant.username }}
